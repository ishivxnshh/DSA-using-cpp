#include <iostream>
#include <map>
using namespace std;
void explainMultiMap()
{
    // everything same as map, only it can store multiple keys, and mpp[key] cannot be used.
}

int main()
{
    explainMultiMap();
}