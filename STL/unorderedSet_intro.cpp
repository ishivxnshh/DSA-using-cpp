#include <iostream>
#include <unordered_set>
using namespace std;
void explainUnorderedSet() //(unique but not sorted)
{
    //lower_bound and upper_bound functions does not work, rest all functions are same
}

int main()
{
    explainUnorderedSet();
}